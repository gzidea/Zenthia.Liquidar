## $CodeExampleName$

In this and the following step, we create an `XtraUserControl` that acts as a splash screen and appears when the application starts. 
Just like the main app form, create it at design time and add a new `MvvmContext` component. 
This form includes a `greetLabel` LabelControl. This label will show a welcome message from a bound ViewModel property.

[Documentation - XtraUserControl](https://docs.devexpress.com/WindowsForms/114564/controls-and-libraries/forms-and-user-controls/xtrausercontrol)